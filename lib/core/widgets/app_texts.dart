import 'package:flutter/material.dart';

class AppTexts extends StatelessWidget {
  const AppTexts({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      width: 40,
      color: Colors.black,
    );
  }
}
